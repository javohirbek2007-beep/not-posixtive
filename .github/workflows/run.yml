name: Run HTB Exploit

on:
  workflow_dispatch:

jobs:
  exploit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pwntools

      - name: Run exploit
        run: python speedrun.py
